巡查派单（farm_dispatch）1.0.9

【修复】pageHtml is not a function（升级后 Node 缓存了旧 _shell）
【修复】CSP 导致一直加载中 → 外链 static/*.js

【安装】
1. 上传 farm_dispatch-1.0.9.zip → 启用（建议先停用再启用，或重启服务）
2. 设置 → 巡查派单 → 初始化用户组
3. 浏览器强制刷新

【版本】1.0.9 · farm_dispatch
