# hanye 全部功能说明

版本基准：主程序 **5.0.0**（产品最新；开源归档截止 **4.3.3**）。  
本文描述监控台**宿主全部功能**与**示例/集市插件、主题**的详细能力。  
**最新一键安装包**请到官网下载：[https://3dmn.cn](https://3dmn.cn)。  
与市面群控选型对照见 **[竞品对照.md](竞品对照.md)**。

---

## 目录

1. [宿主 · 登录账号与权限](#1-宿主--登录账号与权限)
2. [宿主 · 侧栏与页面](#2-宿主--侧栏与页面)
3. [宿主 · 品牌与添加设备](#3-宿主--品牌与添加设备)
4. [宿主 · 设备列表与批量](#4-宿主--设备列表与批量)
5. [宿主 · 设备详情与控制](#5-宿主--设备详情与控制)
6. [宿主 · 打印审核队列](#6-宿主--打印审核队列)
7. [宿主 · 耗材](#7-宿主--耗材)
8. [宿主 · 监控与摄像头](#8-宿主--监控与摄像头)
9. [宿主 · AI 巡检](#9-宿主--ai-巡检)
10. [宿主 · 告警通知](#10-宿主--告警通知)
11. [宿主 · 代打报价与工具](#11-宿主--代打报价与工具)
12. [宿主 · 软件设置各 Tab](#12-宿主--软件设置各-tab)
13. [宿主 · 主题插件集市与扩展](#13-宿主--主题插件集市与扩展)
14. [宿主 · 部署存储与更新](#14-宿主--部署存储与更新)
15. [插件详表](#15-插件详表)
16. [主题详表](#16-主题详表)

---

## 1. 宿主 · 登录账号与权限

### 1.1 登录
- 本地用户名 + 密码登录；JWT 会话。
- 默认管理员：`admin` / `admin123`；首次登录可强制改密（`ForceChangePasswordGate`）。
- 支持远程服务地址（连接自建/局域网监控台后端）。
- 插件可改造登录页（槽位 `login.page` / `login.page.replace`）。

### 1.2 用户与等级
- 用户 CRUD（管理员）；显示名、用户名、密码、等级。
- 等级常见：`admin`（全权限）、`operator`、`viewer` 等。
- 单用户可勾选细粒度权限码（`device.*`、`nav.*`、`filament.*`、`plugin.*` 等）。

### 1.3 用户组（类 Discuz）
- 默认组示例：
  - **操作员**：设备监控与常用控制（查看/创建/编辑/发现/批量，暂停继续取消打印，看摄像头，耗材查看等）
  - **仓管**：耗材增删改绑解绑、报价工具相关导航
  - **只读**：设备/监控/耗材查看、摄像头查看
- 组可配置：权限码列表 + **插件模块白名单**（`moduleAccess`：pluginId + module）。
- 有效权限 = 用户自身权限 ∪ 所属组权限；管理员旁路模块白名单。

### 1.4 设备 ACL
- `deviceAcl`：用户只能看到/操作授权设备（可按设备、按分组）。
- 新建设备自动授予创建者 ACL。
- 用户管理支持分组设备 ACL 批量授权。

### 1.5 企业对接（软件设置 · 企业）
- **企业微信**：启用、CorpId、AgentId、Secret、回调等；扫码登录。
- **钉钉**：启用与应用凭证；扫码登录。
- **AD/域**：启用后可用域账号密码登录。
- 策略：强制绑定对接账号；强制对接登录（禁本地密码）；允许开发确认（内网模拟）。

### 1.6 第三方登录插件扩展
- 可由 `qq_wechat_login` 提供 QQ 互联 / 微信网站应用 OAuth（见插件详表）。

---

## 2. 宿主 · 侧栏与页面

### 2.1 内置导航项
| 导航 | 权限 | 功能 |
|------|------|------|
| FDM | nav.devices | FDM 设备网格 |
| 光固化 | nav.devices | 光固化设备网格（与 FDM 隔离） |
| 耗材管理 | nav.filament | 耗材库与绑定 |
| 常用工具 | nav.tools | 代打报价计算器 |
| 内部监控 | nav.monitor | 监控墙多路摄像头 |
| 区域监控 | nav.monitor | 分区摄像头管理 |
| 模型网站 | — | 第三方模型站外链合集 |
| AI 建模网 | — | AI 建模站外链合集 |
| 软件设置 | nav.settings | 多 Tab 设置 |

### 2.2 其它页面
- 打印审核队列页（申请/通过/驳回/开打）。
- 用户与权限管理页。
- 报价历史页。
- 插件注册的侧栏模块页（默认侧栏内 iframe 镶嵌；切片等可 `openMode: standalone` 全屏打开）。
- 自定义 HTML 页、外链、文件夹导航（软件设置 · 导航可配）。

### 2.3 导航扩展
- 管理员可配置自定义导航树。
- 插件 `patchNav` / `hideNavKeys` 可增删改导航项。

---

## 3. 宿主 · 品牌与添加设备

### 3.1 支持品牌与连接方式
| 品牌 | 方式 | 要点 |
|------|------|------|
| 拓竹 Bambu | 局域网 / 云 | 访问码、MQTT、FTPS 传文件；开发者模式说明；云拉设备 |
| 创想 Creality | 云 / 局域网 | 云 Token+UID 批量添加；局域网走 Moonraker/Fluidd |
| Klipper | Moonraker | URL、API Key、登录探测；Fluidd/Mainsail |
| Qidi 等 | Moonraker | 与 Klipper 同类 |
| Elegoo | 局域网 | 光固化 SDCP 协议 |
| Anycubic | 云 / 局域网 | 云拉设备或本机地址；支持切片软件 access_token 换票 |
| 闪铸 Flashforge | 局域网 | :8898 探测添加 |
| Snapmaker | 局域网 | **U1**：`:7125` Moonraker（与 Klipper 同通道，添加时自动识别）；旧 Artisan 等：Luban Token（触摸屏授权） |

### 3.2 添加设备能力
- 手动填表：名称、品牌、地址、密钥/访问码、分组、标签、技术类型（FDM/光固化）。
- **Klipper 专属（仅添加时）**：可填「自定义品牌名」（卡片角标文案）、可选「自定义品牌角标背景色 / 字体色」；添加后不可再改；其它品牌无此选项。
- **局域网扫描**：按品牌、按 `LAN_SCAN_SUBNETS` 多网段扫描，一点添加。
- 云批量：拓竹云、创想云、Anycubic 云拉列表勾选添加。
- 添加后列表立即显现；插件可扩展品牌表单槽位（`device.add.*`）。

---

## 4. 宿主 · 设备列表与批量

### 4.1 卡片展示
- 健康点（在线/打印中/错误等）、状态文案、进度条、层数、速度、剩余时间。
- 温度：喷头、热床、仓温（有则显示）。
- FDM/光固化芯片、品牌芯片、多选框。
- **Klipper**：若添加时写了自定义品牌名 / 角标背景色 / 角标字体色，右上角品牌标签按设置显示；未设置则保持默认 `.brand-chip` 样式。其它厂家不可自定义。
- 耗材色点（卡片页脚）。
- 插件槽位：`device.card.after-name` / `temps` / `extra` / `footer` / `before` 等。

### 4.2 交互
- 点击打开详情抽屉；品牌过滤条；分组筛选。
- 分页；插件可重排设备顺序（如报错置顶）。

### 4.3 批量操作栏
- 多选后：批量暂停 / 继续 / 停止。
- FDM：批量导入 G-code / `.gcode.3mf` 并打印。
- 光固化：批量控制可用；切片上传对接中（与 FDM 隔离不混用）。
- 权限：`device.batch`、`device.action.*`。

---

## 5. 宿主 · 设备详情与控制

### 5.1 详情内容
- 实时状态、消息、进度、温度、层数。
- 摄像头预览（本机发现 + 第三方插件摄像头可切换）。
- 文件列表 / 上传切片并开打（拓竹更推荐 `.gcode.3mf`）。
- 耗材绑定信息、AMS 槽位展示（拓竹）。
- 详情顶栏可设 **机型**；Klipper 自定义品牌名 / 角标颜色仅在添加时设定，之后不可改。

### 5.2 控制动作（能力探测驱动按钮显隐）
- 暂停 / 继续 / 取消 / 开打指定文件。
- 点动（轴向移动）、回零类（视固件）。
- 挤出 / 回抽（温度可选）。
- 仓温设置、流量、Z-offset。
- 重启 / 固件重启（Moonraker 类）。
- 任意 G-code / Moonraker 脚本透传（需权限）。
- 进退料相关（视品牌）。

### 5.3 安全与持久化
- 细粒度权限：`device.action.pause|resume|cancel|print|camera.view|gcode|moonraker` 等。
- 拓竹访问码等经 `SECRETS_MASTER_KEY` 加密持久化。
- 插件可整页替换详情（如 `detail_console`）。

---

## 6. 宿主 · 打印审核队列

- 用户提交打印申请（文件、目标设备/备注等）。
- 管理员/有权限者：通过、驳回、开打、取消。
- 服务端钩子供插件拦截或 enrichment：`print_request_list|create`、`print_approve|reject|start|cancel`。
- 与农场派单、打印记录等插件联动。

---

## 7. 宿主 · 耗材

### 7.1 基础
- 耗材条目：材料类型、颜色、重量/剩余、供应商、备注等 CRUD。
- 设备绑定 / 解绑；按机查看绑定料。
- 低库存阈值与通知联动。
- 打印完成可自动扣减（软件设置可关）。

### 7.2 拓竹 AMS / 云耗材
- 同步拓竹云料信息；槽位颜色/类型展示。
- 绑定耗材色型可同步到 AMS 显示。

### 7.3 外部耗材库同步（耗材管理内）
- 入口在 **耗材管理**（本地耗材模式顶部栏），不在软件设置。
- **同一时间仅可对接 1 个**外部库，类型三选一：
  - **Spoolman**：地址 + 双向/仅拉/仅推；REST 同步。
  - **Mars Hub**（[hcgl.top](https://hcgl.top/spool/summary)）：站点地址 + 用户中心 API Key；开放查询接口，**仅拉取**。
  - **耗材管家**：自托管地址（默认 `:8321`）+ 登录账号；整包 `/api/data` 同步（建议先仅拉取，避免与拓竹扣减重复记账）。
- **一键同步耗材**：按「品牌 + 材质 + 色值」匹配；颜色名会尽量映射/派生色值。
- 旧版多槽 / 仅 Spoolman 配置读入时自动收敛为 1 个。

---

## 8. 宿主 · 监控与摄像头

### 8.1 内部监控墙
- 多设备摄像头格子；快照/流；并发数可配。
- 失败摄像头可隐藏，减少占位。
- 插件第三方摄像头可并入墙。
- **可纳入 AI 巡检**（本地 YOLO / 云端视觉 API，见 §9）。

### 8.2 区域监控
- 自定义区域；区域内摄像头编排。
- 分区写权限 ACL。

### 8.3 设备摄像头
- 自动发现（Moonraker webcam、创想、Fluidd 优先 :4408 等）。
- 拓竹 X1 舱内流依赖本机 ffmpeg（关于页检测）。
- 快照请求 SSRF 白名单防护。
- 第三方 HTTP/MJPEG 由插件 `extra_cameras` 管理。

---

## 9. 宿主 · AI 巡检

- 总开关；按设备开关。
- 巡检间隔（秒）、最低置信度。
- 异常种类：炒面、空打、模型掉落、翘边；可分别配置动作（仅告警 / 暂停 / 停止）。
- **本地 YOLOv8**：侧重「炒面」检测（模型路径如 `yolo/best.pt`，可配 Python 与权重）。
- **云端 AI API**：OpenAI 兼容视觉接口（`cloudBaseUrl` / API Key / 模型）；可识别全部异常种类；与本地 YOLO 可分别启用。
- 抓拍源：内部监控与可参与 AI 的第三方摄像头。

---

## 10. 宿主 · 告警通知

### 10.1 本机偏好（软件设置 · 设置）
- 设备异常系统通知、打印完成通知、空闲通知、低库存通知。

### 10.2 通道（软件设置 · 告警）
- 企业微信 / 钉钉 / 自定义 Webhook。
- 短信：创蓝等预设或自定义 API URL/Method/Body/Headers。
- 按事件类型开关；测试发送；历史记录与重试。
- 相关操作受告警/管理员权限约束。

---

## 11. 宿主 · 代打报价与工具

- **常用工具**：导入切片 G-code 估算用量/时长相关参数；功率、人工、其它字段；方案保存与列表；结果对比与拆分展示。
- **报价历史**：历史记录查询与管理。
- 全流程含大量 `tools.*` / `quote.*` 插件槽位可扩展。

---

## 12. 宿主 · 软件设置各 Tab

| Tab | 详细内容 |
|-----|----------|
| 设置 | 状态刷新间隔；监控墙快照并发；局域网扫描网段；开机自启；关闭到托盘；异常/完成/空闲/低库存通知；耗材自动扣减；配色主题预设；背景色/背景图；数据目录与下载子目录、迁移；备份/恢复（可选含 secrets） |
| 品牌 | Logo、品牌名等展示定制 |
| 导航 | 自定义侧栏：文件夹、内置页、外链、自定义页 |
| 报价历史 | 报价相关入口/设置 |
| 用户 | 用户、用户组、权限、设备 ACL |
| 打印审核 | 审核队列相关策略开关 |
| 企业 | 企微/钉钉/AD 与强制策略 |
| AI | 巡检：本地 YOLO + 可对接 OpenAI 兼容视觉 API；异常动作 |
| 告警 | 多通道告警 |
| 主题 | 上传/启用/卸载主题包 |
| 应用集市 | 登录集市账号；浏览安装更新；封面；多 Base |
| 插件 | 上传 zip、启用禁用、变量编辑、卸载 |
| 插件设置 | 各插件配置归并页 |
| 关于 | 未授权：机器码 + 四级方案卡片（同步授权系统）+ 激活；已授权：仅机器码与到期时间；超限添加设备时可跳转本页 |

---

## 13. 宿主 · 主题插件集市与扩展

### 13.1 主题包
- 支持 `siteMode: full` 整站换壳；`.htm` 模板、extends/block。
- 与默认侧栏卡片墙可完全不同的布局（见主题详表）。

### 13.2 插件微内核
- 服务端钩子：登录用户权限、设备状态控制、耗材、监控、打印审核、设置告警、UI 资产等。
- 浏览器 `HanyePlugin`：槽位、导航补丁、权限补丁、添加设备扩展、事件。
- 模块页 type：`page` / `admin` / `cron`；默认镶嵌打开；需系统文件选择器时模块可设 `openMode: "standalone"`（如 Orca 切片）。
- 权限目录 `permissions_catalog`；插件私有权限码。

### 13.3 应用集市
- 默认地址由 `MARKET_BASE_URL` 配置（常见同机 **:3001**，与官方席位 **:3100** 分离）。
- 多节点 failover；本地版本与集市版本对比「更新」。
- **只管插件/主题**正版心跳（`deviceId` + 用户 `ownedApps`）；**不管**打印机台数。
- 开发者可在集市上传插件/主题；官方席位授权系统不对外、不在集市展示。
- 开发文档：`{MARKET_BASE_URL}/docs`（上架 + 插件/主题手册）；开发者中心：`/developer`。

---

## 14. 宿主 · 部署存储与更新

### 14.1 运行方式
- **工程目录**：monorepo 下 **`主程序/`**（含 `package.json` / `src`；**不含** docker / node.js20 等安装脚手架目录）；仓库根为文档与官网/验证/集市等并列项目。
- **Node 直装**：进入 `主程序/`，Node 20+，`npm run build && npm start`，默认端口 **17890**；常驻可用 PM2 等（仓库不附带启停脚本目录）。
- 数据：默认 JSON `主程序/data`；可选 MySQL（`USE_MYSQL` 等环境变量）。
- **Docker**：可自行编排容器部署（仓库不附带 `docker/` 目录）；日常推荐官网一键包。
- **一键包**（官网 3dmn.cn）：Windows exe、macOS dmg、Linux deb/rpm、飞牛 fpk、群晖 spk（内置或依赖 Node 视平台）。

### 14.2 更新与授权
- **开源截止 v4.3.3**：GitHub / Gitee / GitCode 仅归档至该版本；其后新版本**不开源**。
- **新版本 / 一键包**请到官网 [https://3dmn.cn](https://3dmn.cn) 下载；关于页**仅展示**当前版本与可查到的最新版本，**不再提供应用内一键更新**。
- **官方席位授权**：对接官方验证系统（默认 :3100，**不对外公开**）。关于页未授权时展示四级方案卡片（台数 / 标价 / 授权天数，来自授权后台「产品」与公开目录 API）；**激活后仅显示机器码与到期时间**。授权到期或降级为免费档时，超出上限的设备在列表中**隐藏**（磁盘数据保留），重新激活后自动全部显示。与应用集市（:3001）分离。
- **价格说明**：官网 [3dmn.cn](https://3dmn.cn)「价格」页从授权系统目录 API 拉取四级方案；页内可联系 QQ 开通。集市第三方插件可能另收费。
- **正版查询**：官网 [3dmn.cn/verify.php](https://3dmn.cn/verify.php) 输入机器码，调用授权系统 `POST /api/v1/license/machine-status` 查询是否正版、方案与到期时间（不展示授权码）。

### 14.3 备份
- 导出/导入备份包；可选包含 secrets（访问码等，需妥善保管）。含 `seat-license.json`。

---

## 15. 插件详表

下列为仓库根目录 **`插件源码/`** 中的插件（可上传集市或本地安装）。版本以各包 `plugin.json` 为准。

### `app_launcher` · 悬浮快捷启动 · **1.2.0**

**功能概述：** 悬浮快捷启动：本机隐藏助手（Win EXE / Mac App）绑定当前用户后，网页点击只打开自己电脑上的软件。

**详细能力：**
- 网页悬浮按钮打开本机已绑定软件（需本机隐藏助手 Win/Mac）。
- 按用户隔离；可限显示个数、角落、图标自定义、是否允许服务端本机打开。

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `enabled` | boolean | `1` | 启用悬浮快捷启动 |
| `show_limit` | number | `0` | 默认显示个数（0=全部） |
| `fab_size` | number | `56` | 悬浮按钮边长(px) |
| `fab_corner` | text | `br` | 默认角落（br右下 / bl左下 / tr右上 / tl左上） |
| `allow_server_open` | boolean | `1` | 允许服务端本机打开（桌面同机有效；远程 Docker 无效） |
| `allow_user_fab` | boolean | `1` | 允许每人自定义悬浮图标（关闭则只用管理员全局图） |

**钩子：** `register`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `capability_kit` · 能力原语示例 · **1.0.0**

**功能概述：** 演示宿主 v1.1 插件原语：能力探测、传文件开打、锁机、快照、打印事件写历史。非业务成品。

**详细能力：**
- 开发者示例：能力探测、上传开打、锁机、快照、打印事件写历史。非生产业务插件。

**钩子：** `register`

**前端资源：** client.js

---

### `card_model_portrait` · 机型肖像卡片 · **1.4.3**

**功能概述：** 本地离线机型肖像；软件设置可补充/覆盖机型 PNG（修复上传预览与卡片显示）。Klipper 若添加时写了自定义品牌名，肖像标题「品牌 · 机型」会显示该名称。

**详细能力：**
- 按机型显示离线肖像图；软件设置可上传覆盖 PNG。

**钩子：** `register`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `card_progress_vivid` · 设备进度条醒目 · **1.2.0**

**功能概述：** 加粗进度条；用持续爬行的虫子标记当前进度（不显示%）。

**详细能力：**
- 加粗进度条；进度前端「爬行虫子」动效与光带（不显示百分比数字）。

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `show_percent` | boolean | `1` | 显示爬行虫子（跟在进度前端） |
| `animate` | boolean | `1` | 虫子持续爬行 + 进度光带 |
| `thick` | boolean | `1` | 进度条加粗 |

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `card_status_vivid` · 设备卡片状态醒目 · **1.0.0**

**功能概述：** 强化设备卡片健康/打印状态显示：大号徽章、色条、脉冲动画，一眼可辨。

**详细能力：**
- 大号健康徽章、色条、打印中/异常脉冲，强化状态可读性。

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `show_badge` | boolean | `1` | 名称旁显示健康徽章（在线/离线等） |
| `show_banner` | boolean | `0` | 额外状态条（默认关，避免与卡片底栏重复） |
| `animate` | boolean | `1` | 打印中/异常脉冲动画 |

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `chamber_temp` · 仓内温度显示 · **1.3.0**

**功能概述：** 在设备列表卡片显示仓内温度与高温高亮；不占用侧栏导航。设置见「软件设置 → 插件 → 变量」。

**详细能力：**
- 卡片温度区仓温药丸；名称旁仓温徽章；可选列表上方总览条。
- 超过阈值高温高亮；轮询间隔可配。

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `show_on_cards` | boolean | `1` | 设备卡片显示仓温 |
| `show_badge` | boolean | `1` | 设备名称旁显示仓温徽章 |
| `show_panel` | boolean | `0` | 列表上方显示仓温总览条 |
| `warn_celsius` | number | `45` | 高温预警阈值（℃） |
| `poll_sec` | number | `5` | 总览条刷新间隔（秒） |

**钩子：** `register`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `company_chat` · 公司聊天 · **1.1.0**

**功能概述：** 登录后侧栏公司聊天：在线成员、全员/私聊、发文件；消息可查询；可配置保留天数自动清理。

**详细能力：**
- 登录后右侧聊天栏：在线成员、全员频道、私聊、发文件。
- 消息可查；定时清理任务；私聊策略与保留天数可配。

**模块 / 页面：**

| 菜单 | 模块名 | 类型 | 顺序 | 权限 |
|------|--------|------|------|------|
| 聊天清理 | `cleanup` | cron | 0 | `adminOnly` |

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `dm_policy` | text | `all` | 私聊权限（all=全员可私聊 / admin_only=仅管理员可发起私聊） |
| `retain_days` | number | `30` | 聊天记录保留天数（超时自动清理） |
| `cleanup_enabled` | boolean | `1` | 启用自动清理 |
| `panel_default_open` | boolean | `0` | 登录后默认展开聊天侧栏 |
| `poll_sec` | number | `3` | 消息轮询间隔（秒） |
| `max_len` | number | `2000` | 单条消息最大字数 |
| `allow_file` | boolean | `1` | 允许发送文件 |
| `max_file_mb` | number | `10` | 单个文件最大 MB |

**钩子：** `register`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `detail_console` · 详情控制台 · **1.5.0**

**功能概述：** 替换设备详情：多路摄像头 + 点动 + Mainsail 料盘 SVG + G代码入队；每台设备可配置 Fluidd/Mainsail 等后台地址并一键打开。

**详细能力：**
- 可替换宿主默认详情控制区。
- 多路摄像头切换；点动面板；Mainsail 风格料盘 SVG；G-code 入队。
- 每台设备可配置并一键打开 Fluidd/Mainsail 等后台地址。

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `replace_detail` | boolean | `1` | 替换整页详情控制区 |
| `poll_ms` | number | `2000` | 状态刷新间隔(ms) |
| `default_temp` | number | `220` | 进退料默认温度 |

**钩子：** `register`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `device_maintain` · 设备保养提醒 · **1.0.0**

**功能概述：** 按打印小时累计保养周期；到期在卡片贴「需维护」标签；点标签或页内「维护完成」重置计时。未到期不显示。

**详细能力：**
- 监听设备状态，仅在「打印中」累计真实打印秒数并换算小时。
- 达到周期后在设备卡片名称旁显示可点击「需维护」徽章；未到期完全不显示。
- 确认维护完成后该机累计清零，并记录上次维护时间。
- 侧栏「设备保养」：到期列表 / 全部设备、改单机周期、维护完成 / 提前完成。

**模块 / 页面：**

| 菜单 | 模块名 | 类型 | 顺序 | 权限 |
|------|--------|------|------|------|
| 设备保养 | `board` | page | 45 | `plugin.device_maintain.view` |

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `enabled` | boolean | `1` | 启用保养提醒 |
| `interval_hours` | number | `100` | 默认保养周期（打印小时，到点贴标签） |
| `show_card_badge` | boolean | `1` | 到期时在设备卡片显示「需维护」 |

**钩子：** `register`, `statuses_publish`, `ui_assets`, `permissions_catalog`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `device_model_card` · 设备卡片机型显示 · **1.0.0**

**功能概述：** 在设备列表卡片上显示机型；未设置时显示「未知」。

**详细能力：**
- 卡片显示机型名；位置可选名称旁/扩展区/两处；未知机型显示「未知」。

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `show_on_cards` | boolean | `1` | 设备卡片显示机型 |
| `place` | select | `after_name` | 显示位置 |
| `poll_sec` | number | `15` | 机型缓存刷新间隔（秒） |

**钩子：** `register`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `device_force_order` · 设备强制排序 · **1.0.4**

**功能概述：** 按你设定的序号强制排列设备；开启后优先于「设备状态置顶」。其它插件（含 FDM 机群紧凑卡片）通过 `devices_list` 钩子跟随此顺序。

**详细能力：**
- **软件设置 → 强制排序**：拖拽 / 填序号 / 上下移动；可「用当前设备列表初始化」。
- 列表展示 **名称 · 机型 · IP**（不再只显示设备 ID）。
- 开关与顺序写入 `order.json`；前端正确解包宿主 `{ ok, data }` 响应，开启/关闭可持久生效。
- 开启后通过 `__hanyeForceDeviceOrder` 通知「状态置顶」让路，避免两边轮流改序导致卡片跳来跳去。
- 主界面卡片：`setDeviceOrder` + CSS `order`。
- 服务端 `devices_list`（`hookPriority: 900`）统一列表序。
- API：`GET/PUT /api/v1/device-force-order`、`POST /api/v1/device-force-order/move`。

**可配置变量（软件设置 → 插件 / 强制排序 Tab）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `enabled` | boolean | `1` | 启用强制排序（亦写入 `order.json`） |
| `append_unknown` | boolean | `1` | 未入序设备放在末尾 |

**钩子：** `register`, `devices_list`（优先级 900）

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `device_priority` · 设备状态置顶 · **1.0.1**

**功能概述：** 报错设备自动排到最前（后报错的更靠前）；打印完成同样按发生时间排列，排在报错之后。若已启用「设备强制排序」且有序号，本插件自动让路（前端 + `devices_list`），不再改序。

**详细能力：**
- 根据报错/完成发生时间重排设备卡片（含分页第一页）。
- 报错优先于完成；同类中越新越靠前。
- 与强制排序共存时不互抢，避免卡片闪跳。

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `enabled` | boolean | `1` | 启用自动重排 |
| `pin_error` | boolean | `1` | 报错置顶（后发生的更靠前） |
| `pin_finished` | boolean | `1` | 打印完成排列（后完成的更靠前，排在报错后面） |

**钩子：** `register`, `devices_list`, `statuses_publish`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `extra_cameras` · 设备第三方摄像头 · **1.0.1**

**功能概述：** 为每台设备添加 HTTP/MJPEG 第三方摄像头：按钮弹窗添加、详情左右滑动切换、内部监控墙显示，并纳入 AI 巡检。

**详细能力：**
- 为任意设备添加 HTTP/MJPEG 地址；详情左右滑动切换。
- 进入内部监控墙；可选默认参与 AI 巡检。

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `show_settings` | boolean | `1` | 详情页显示摄像头设置 |
| `default_ai` | boolean | `1` | 新增摄像头默认参与 AI 巡检 |

**钩子：** `register`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `farm_dispatch` · 巡查派单 · **1.2.0**

**功能概述：** N份连续生产、取件/清床两步确认、错峰开打、中央文件库；智能派单与开打拦截。

**详细能力：**
- **巡查看板**：手机/桌面查看设备状态；报错/完成筛选；设空闲、标维修等巡查动作。
- **派单审核**：待审核工单通过/驳回；与开打拦截联动。
- **提交打印**：上传或选用文件提交生产任务；支持份数。
- **中央文件库**：集中存放已切片文件，供派单选用。
- **派单日志**：操作审计。
- **N 份连续生产**：清床确认后自动派下一份，直到份数完成。
- **两步确认**：打印完成 → 先确认取件 → 再确认清床/空闲（可关）。
- **智能派单 / 拦截**：不可用状态（维修、报错、完成未确认、打印中、离线、未绑耗材等）禁止开打；可要求必须绑耗材；**空闲机可自动领/续派任务**（含 N 份连续生产清床后自动派下一份）。
- **错峰开打**：相邻开打间隔秒数，降低同时加热跳闸风险。

**模块 / 页面：**

| 菜单 | 模块名 | 类型 | 顺序 | 权限 |
|------|--------|------|------|------|
| 巡查看板 | `patrol` | page | 38 | `plugin.farm_dispatch.patrol` |
| 派单审核 | `audit` | page | 39 | `plugin.farm_dispatch.audit` |
| 提交打印 | `submit` | page | 40 | `plugin.farm_dispatch.submit` |
| 中央文件库 | `files` | page | 41 | `plugin.farm_dispatch.files` |
| 派单日志 | `logs` | page | 42 | `plugin.farm_dispatch.logs` |

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `block_unavailable` | boolean | `1` | 拦截不可用设备开打（维修/报错/完成未确认/打印中/离线/未绑耗材） |
| `require_filament` | boolean | `1` | 智能派单要求设备已绑定耗材 |
| `pickup_two_step` | boolean | `1` | 打印完成需两步：先确认取件，再确认清床/设空闲 |
| `stagger_enabled` | boolean | `1` | 错峰开打（相邻两次开打间隔 stagger_sec 秒，防同时加热跳闸） |
| `stagger_sec` | number | `45` | 错峰间隔秒数（默认 45） |
| `max_file_mb` | number | `80` | 申请/文件库上传最大 MB |
| `log_max` | number | `5000` | 操作日志最多保留条数 |

**钩子：** `register`, `control_before`, `print_batch_before`, `print_start`, `print_request_create`, `statuses_publish`, `ui_assets`, `permissions_catalog`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `fdm_fleet_cards` · FDM 机群紧凑卡片 · **1.0.4**

**功能概述：** 保留原 FDM 卡片；一键切换超紧凑机群视图（温度/进度/剩余时间），完成/报错盖章，完成或报错时语音播报。机群顺序跟随 `devices_list`（含强制排序 / 状态置顶）。

**详细能力：**
- 一键在经典卡片与超紧凑机群视图间切换。
- 紧凑视图突出温度/进度/剩余时间；完成或报错盖章；可语音播报。
- 快照 API 经 `api.runHook('devices_list')` 再过滤 FDM，与主界面顺序一致。

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `voice_announce` | boolean | `1` | 完成/报错语音播报 |
| `default_compact` | boolean | `0` | 进入 FDM 页默认紧凑视图 |

**钩子：** `register`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `demo_kernel_v2` · 微内核 v2 示例 · **1.1.0**

**功能概述：** activate(ctx) + 领域事件 + 签名回调 + cache 演示

**详细能力：**
- 微内核 v2 演示：activate、领域事件、HMAC 回调、cache；可改横幅文案。

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `banner` | text | `Kernel v2 已激活` | 横幅文案 |
| `callback_secret` | text | `` | 回调 HMAC 密钥（空则不校验） |

**钩子：** `filter:devices.list`, `filter:ui.nav`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `slice_node` · 自建云切片 · **1.2.9**

**功能概述：** 用户自建切片节点。Windows 一键 EXE（内置 Node.js、浏览器配置面板（本机 http://127.0.0.1:17991/）、TightVNC + websockify + noVNC）请到官网 **[3dmn.cn](https://3dmn.cn)** 下载（文件名 `slice-node-agent-setup.exe`），**不打进插件包**。可选 CLI 云切片，或打开完整界面（默认**专属窗口串流**多人互不可见；可切换整桌 noVNC）。

**详细能力：**
- 管理员创建节点令牌；云切片页引导至 [3dmn.cn](https://3dmn.cn) 下载一键包；插件内仅保留高级脚本 zip。
- 配置面板：监控台地址 / nodeId·token / 本机 IP·端口 / 切片软件 exe / 启停 Agent / 安装 VNC / 防火墙。
- Agent 扫描或手动路径；心跳合并网页手动路径。
- **CLI 云切片**；**专属窗口串流**（6100 起）；**整桌 noVNC**（5900+6080，可选）。
- 看板保留输入草稿、已选模型与进行中窗口会话。
- 与 `orca_web` 互补。

**模块 / 页面：**

| 菜单 | 模块名 | 类型 | 顺序 | 权限 |
|------|--------|------|------|------|
| 云切片 | `board` | page | 34 | `plugin.slice_node.board` |

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `agent_offline_sec` | number | `90` | 节点离线判定（秒） |
| `max_upload_mb` | number | `200` | 模型/结果最大 MB |
| `keep_jobs` | number | `100` | 保留任务条数 |

**钩子：** `register`, `ui_assets`, `permissions_catalog`

**前端资源：** client.js；主题 CSS：`theme.css`；静态脚本包：`static/agent/slice-node-agent.zip`；一键 EXE 仅官网 3dmn.cn

---


### `orca_web` · Orca 网页切片 · **1.0.7**

**功能概述：** 浏览器内 OrcaSlicer（WASM）完整切片；独立插件，切片后可下载 G-code。需监控台 ≥4.1.11 全屏打开插件页。自建多引擎云切片见 **`slice_node`**。

**详细能力：**
- 浏览器内 OrcaSlicer WASM 完整切片；侧栏「Orca 切片」全屏页。
- 切片完成后可下载 G-code；需监控台 ≥4.1.11 全屏打开才能正常选文件。
- WASM 模式 auto/单线程/多线程。

**模块 / 页面：**

| 菜单 | 模块名 | 类型 | 顺序 | 权限 |
|------|--------|------|------|------|
| Orca 切片 | `slice` | page | 35 | `plugin.orca_web.slice` |

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `wasm_mode` | string | `auto` | WASM 模式：auto / st / mt |

**钩子：** `register`, `ui_assets`, `permissions_catalog`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `print_order` · 打印单进度 · **1.3.0**

**功能概述：** 卡片管理打印单。一单可设多种**盘型**（如 30A+30B、单打 A、20A+30B）；各盘型单独记已打盘数、一盘料/时间与盘内配比。自动汇总各模型已出/合格/剩余；剩余盘/料/时间在「能单独做完全部剩余」的盘型中取最少者约估。可设耗材并联动库存提醒。

**详细能力：**
- 侧栏「打印单」卡片列表：进行中 / 暂停 / 已完成筛选。
- **模型清单**：目标件数 + 不合格补录（不计入合格）。
- **多盘型**：每种盘型含名称、盘内配比（模型 × 每盘件数）、一盘料、一盘时间、已打盘数；同一单可并存多种排盘。
- 推算：`已出(模型) = Σ(盘型已打盘数 × 该盘型内该模型件数)`；`合格 = 已出 − 不合格`；剩余 = 目标 − 合格。若无一盘型能单独覆盖全部剩余，汇总剩余盘显示「—」并提示需组合打。
- 旧单自动迁移：原先「模型一盘件数 + 整单已打盘数」→ 一个「默认盘」盘型。
- **不合格补录**：卡片上可直接改；坏件不计入合格。
- **耗材**：单可设种类、颜色名、色值；可从耗材库点选卷料自动填入并绑定。
- **联动耗材库**（插件变量 `link_filament`）：读取宿主 `filament-spools.json`；按种类/颜色（或绑定卷）汇总库存余量，与本单约估剩余料（g/kg）对比；无匹配或余量不足时卡片显示「耗材可能不足」；关闭联动则不提示。
- 卡片上可按盘型改已打盘数并更新；支持暂停 / 标完成 / 重开 / 删除。
- 数据存 `data/plugin-data/print_order/orders.json`。

**模块 / 页面：**

| 菜单 | 模块名 | 类型 | 顺序 | 权限 |
|------|--------|------|------|------|
| 打印单 | `board` | page | 42 | `plugin.print_order.view` |

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `enabled` | boolean | `1` | 启用打印单 |
| `link_filament` | boolean | `1` | 联动耗材库；不足时卡片提醒（关则不提示） |
| `default_material_unit` | text | `g` | 默认料单位 |
| `show_done` | boolean | `0` | 列表默认显示已完成单 |

**权限：** `plugin.print_order.view`（查看）、`plugin.print_order.edit`（新建/改单/删单）

**钩子：** `register`, `permissions_catalog`

**前端资源：** client.js；主题 CSS：`theme.css`；页面脚本：`static/page-board.js`

---

### `print_log` · 打印记录 · **1.0.0**

**功能概述：** 记录谁在何时通过系统或现场打印了什么文件、用了多少材料；可勾选设备；软件设置配置；侧栏导航查看（可开关/授权）。

**详细能力：**
- 记录系统下发与机上操作的打印事件：操作者、时间、设备、文件名、耗材用量等。
- 可记录全部机或 JSON 勾选设备；可关系统/现场某一类。
- 侧栏「打印记录」页查询；超期或超条数自动清理。

**模块 / 页面：**

| 菜单 | 模块名 | 类型 | 顺序 | 权限 |
|------|--------|------|------|------|
| 打印记录 | `page` | page | 35 | `plugin.print_log.page` |

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `show_nav` | boolean | `1` | 显示侧栏「打印记录」导航 |
| `record_all` | boolean | `1` | 记录全部打印机（关闭则仅勾选设备） |
| `device_ids` | text | `[]` | 要记录的设备 ID（JSON 数组，record_all=0 时生效） |
| `record_system` | boolean | `1` | 记录系统发送的打印 |
| `record_on_device` | boolean | `1` | 记录现场/机上操作的打印 |
| `retain_days` | number | `180` | 记录保留天数（0=不自动清理） |
| `max_rows` | number | `2000` | 最多保留条数 |

**钩子：** `register`, `control_before`, `print_request_create`, `print_approve`, `print_start`, `print_batch_before`, `statuses_publish`, `ui_assets`, `permissions_catalog`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `qq_wechat_login` · QQ / 微信登录 · **1.0.0**

**功能概述：** 对接 QQ 互联与微信开放平台网站应用登录；支持绑定/解绑、强制绑定、仅第三方登录或密码+QQ+微信均可登录。

**详细能力：**
- QQ 互联、微信开放平台网站应用扫码/授权登录。
- 登录模式：密码+QQ+微信 / 仅 OAuth / 仅密码。
- 强制绑定、自动建本地用户、默认等级；回调 URL 可配。

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `login_mode` | text | `all` | 登录方式（all=密码+QQ+微信 / oauth_only=仅QQ微信 / password_only=仅密码） |
| `force_bind` | boolean | `0` | 强制绑定：密码登录后必须绑定 QQ 或微信 |
| `auto_create` | boolean | `1` | 首次 QQ/微信登录自动创建本地用户 |
| `default_level` | text | `viewer` | 自动创建用户等级（viewer / operator / admin） |
| `qq_enabled` | boolean | `1` | 启用 QQ 互联登录 |
| `qq_app_id` | text | `` | QQ 互联 AppID |
| `qq_app_key` | text | `` | QQ 互联 AppKey |
| `qq_redirect_uri` | text | `` | QQ 回调地址（须与互联平台一致，默认本站 /api/v1/qq-wx/callback/qq） |
| `wx_enabled` | boolean | `1` | 启用微信开放平台网站应用登录 |
| `wx_app_id` | text | `` | 微信网站应用 AppID |
| `wx_app_secret` | text | `` | 微信网站应用 AppSecret |
| `wx_redirect_uri` | text | `` | 微信回调地址（须与开放平台一致，默认本站 /api/v1/qq-wx/callback/wechat） |

**钩子：** `register`, `login_before`, `login_after`, `auth_me`, `users_list`

**前端资源：** client.js；主题 CSS：`theme.css`

---

### `demo_hello` · 全站改造示例 · **2.0.0**

**功能概述：** 演示插件改登录页、导航、主题、权限与界面嵌入点（仿 DZ 全站可改）

**详细能力：**
- 演示改登录页、导航、权限、主题解析与多钩子；含管理页与演示页。教学用。

**模块 / 页面：**

| 菜单 | 模块名 | 类型 | 顺序 | 权限 |
|------|--------|------|------|------|
| 全站插件设置 | `admin` | admin | 0 | `adminOnly` |
| 插件演示页 | `page` | page | 10 | `plugin.demo_hello.page` |

**可配置变量（软件设置 → 插件）：**

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `greeting` | text | `你好` | 问候语 |
| `prefix_devices` | boolean | `0` | 设备名前缀 |
| `restyle_login` | boolean | `1` | 改造登录页 |
| `hide_models_nav` | boolean | `0` | 隐藏「模型网站」导航 |

**钩子：** `common`, `devices_list`, `ui_assets`, `permissions_effective`, `permissions_catalog`, `theme_resolve`, `register`, `users_list`, `user_create`, `user_update`, `user_delete`, `monitor_camera_snapshot`, `print_request_list`, `print_request_create`, `print_approve`, `print_reject`, `print_start`, `print_cancel`

**前端资源：** client.js；主题 CSS：`theme.css`

---

## 16. 主题详表

### `command_hud` · 指挥舱 HUD · **2.0.0**

siteMode=full 整站指挥舱：顶栏航电条 + 左遥测/右波形侧翼 + 中央环心舞台；设备为密表行，非经典侧栏卡片墙。

- siteMode：`full`

### `fullsite_board` · 整站看板 · **1.0.0**

siteMode=full：主题 .htm 提供整站 HTML 骨架，React 仅挂载到 data-hanye-mount 交互岛。示范「主题即整站」。

- siteMode：`full`

### `lineboard` · 航线看板 · **1.0.0**

与经典监控台完全不同：左侧功能轨 + 密表行列表 + 分栏登录；浅色制图纸风格，锐角卡片与行式设备条。


### `saffron_loft` · 藏红工坊 · **1.0.3**

整站换壳：底栏胶囊导航 + 票券式设备卡 + 藏红/薄荷配色与入场动效。与默认侧栏卡片墙完全不同。

- siteMode：`full`

### `sample_topnav` · 示例·顶栏工单台 · **1.1.0**

Discuz 式 .htm 模板 + topnav 顶栏导航 + list 设备行 + split 登录分栏。templates/ 自动发现，支持 extends/block/include。


---

*文档随主程序与示例插件/主题同步维护：凡增删改优宿主、插件、主题，须更新本文后 `npm run push:mirrors`。版本摘要另见 `更新日记.md`。*
